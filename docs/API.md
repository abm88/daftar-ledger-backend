# Daftar API Reference

The complete API documentation — every endpoint with request payloads and
example responses — lives in the repository README:

**[README → API documentation](../README.md#api-documentation)**

It covers conventions (authentication, error format, pagination) and all
endpoint groups: health, auth, cities, assets, rates, cash drawer,
counterparties, hawalas, customers, transactions, FX trades, investments,
settings, and reports.
